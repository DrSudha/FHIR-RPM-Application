# Copy to .env.local and adjust values.

FHIR_BASE_URL=
FHIR_AUTH_TOKEN=

# Required for portal login sessions (minimum 16 characters).
SESSION_SECRET=change-me-to-a-long-random-secret

# Optional bootstrap admin (created automatically when data/users.json is empty).
ADMIN_EMAIL=admin@prohealth.local
ADMIN_PASSWORD=ProHealth2026!
ADMIN_NAME=Dr. Sarah Mitchell
